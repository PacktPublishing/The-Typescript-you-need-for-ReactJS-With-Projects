/* HELLO WORLD
    Typescript is compilled into Javascript using what we call a typescript compiller (tsc)
    - This simply means typescript runs any where javascript runs
    - To install the typescript compiller we need to install NodeJS (which comes with npm)
    - install typescript: npm install typescript --save-dev
    - typescript commands: npx tsc
    - typescript version: npx tsc -v
    - Create tsc config file: npx tsc --init
    - run "tsc" in the config path, and run "node helloworld" in the build path
*/

console.log("Hello world, super heroes");
